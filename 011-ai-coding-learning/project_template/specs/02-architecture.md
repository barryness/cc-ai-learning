# Architecture: [项目名称]

> Architecture Design Document — 怎么做（宏观）

## 技术选型

| 层级 | 技术 | 版本 | 理由 |
|------|------|------|------|
| 语言 | | | |
| 框架 | | | |
| 数据库 | | | |
| 缓存 | | | |
| 消息队列 | | | |
| 部署 | | | |

## 系统架构图

```
┌─────────────────────────────────────────────┐
│                   [层名称]                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 组件 A   │  │ 组件 B   │  │ 组件 C   │  │
│  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────┘
                      │
┌─────────────────────────────────────────────┐
│                   [层名称]                    │
│  ┌──────────┐  ┌──────────┐                 │
│  │ 组件 D   │  │ 组件 E   │                 │
│  └──────────┘  └──────────┘                 │
└─────────────────────────────────────────────┘
```

## 分层设计

| 层 | 职责 | 依赖方向 |
|----|------|----------|
| Router/Controller | 请求路由、参数校验 | → Service |
| Service | 业务逻辑 | → Repository |
| Repository | 数据访问 | → Database |
| Model | 数据定义 | 被所有层依赖 |

## 数据模型

### [Model Name]
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID/int | 是 | 主键 |
| created_at | datetime | 是 | 创建时间 |
| updated_at | datetime | 是 | 更新时间 |

## API 设计

### [资源名称]

```
POST   /api/v1/[resource]        # 创建
GET    /api/v1/[resource]        # 列表
GET    /api/v1/[resource]/{id}   # 详情
PUT    /api/v1/[resource]/{id}   # 更新
DELETE /api/v1/[resource]/{id}   # 删除
```

### 接口详情

#### POST /api/v1/[resource]
- 功能:
- 认证: 需要/不需要
- 请求体:
```json
{
  "field": "value"
}
```
- 响应:
```json
{
  "data": {},
  "error": null
}
```
- 错误码:

## 代码结构

```
project/
├── src/
│   ├── routers/       # API 路由
│   ├── services/      # 业务逻辑
│   ├── repositories/  # 数据访问
│   ├── models/        # 数据模型
│   ├── schemas/       # 请求/响应 Schema
│   └── utils/         # 工具函数
├── tests/
│   ├── unit/
│   └── integration/
├── specs/             # Spec 文档
├── memory/            # AI Memory
├── CLAUDE.md
├── AGENTS.md
└── README.md
```

## 安全设计

- 认证方案:
- 授权策略:
- 输入校验:
- 敏感数据处理:

## 风险与替代方案

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|----------|
| | | | |
